#include <stdio.h>
#include <stdlib.h>
#include "Readfile.h"


Matrix* BinToMatrix(const char * filename, int inRow, int inCol){
	/*FILE *fptr;
	Matrix* mm = NULL;
	int i=0,j=0;
	
	fptr = fopen(filename, "rb");
	if(!fptr){
		printf("\nError : ÀÉ®×¶}±Ò¥¢±Ñ\n")
	}
	else{
		mm = new Matrix(inRow, inCol);
	}
	while(!feof(fptr)){
		if(j<inCol){
			
		}
	}
	
	return mm;*/
} 
